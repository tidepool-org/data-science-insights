# PLN-1008 Project History

Running changelog of design decisions, scope changes, and major commits
for the No Meal Announcement analysis. Update on every non-trivial change.

## 2026-05-21 — Project initialized

- Created `no_meal_announcement/` skeleton mirroring `FDA_real_world_data/`.
- Drafted folder structure under Phase A: `data_staging/`, `analysis/`,
  `testing/`, `exploratory/`, `docs/`.
- All `data_staging/*.py` and `analysis/**/*.py` modules created as Phase A
  stubs (function signatures + docstrings, `raise NotImplementedError`).
- All `testing/**/test_*.py` files created as Phase B placeholders with
  `@pytest.mark.skip` markers and planned test cases listed in docstrings.
- `architecture.md` initialized; column dictionary deferred to Phase C.
- Plan stored at `/Users/mconn/.claude/plans/we-re-going-to-do-purring-fern.md`.

## Confirmed decisions (from 2026-05-21 planning conversation)

- **Skeleton**: FDA-style. Rationale: regulatory parity with PLN-1001.
- **Cohort**: Reuse PLN-1001 inclusion criteria verbatim (DIY Loop,
  Loop <3.4.0, PAF=0.4, age ≥6, ≥10 user-days, ≥70% CGM coverage).
- **Code reuse**: Import directly from `FDA_real_world_data` rather than
  forking. Single source of truth for shared utilities.

## 2026-05-30 — Pipeline implemented; §8.1 Method A landed

- **data_staging/** all current scripts implemented except the stub-style analysis-ready skeleton was replaced with a real implementation:
  - `export_user_day_cbg`, `compute_user_day_glycemic_endpoints` (wrap FDA helper unmodified), `export_user_day_bolus_counts`, `export_user_day_carbs`, `export_user_day_tdd`, `export_user_day_age`, `export_user_day_classification`, `export_user_day_analysis_ready`.
- **Day universe** is `dev.fda_510k_rwd.loop_recommendations` (one row per user-day with a known dosing decision per §7.3). Bolus counts, carbs, classification, analysis-ready, age all anchor on it.
- **TDD = delivered**: HealthKit `rate × LEAST(gap_to_next, duration)` as primary, Loop-direct `payload.deliveredUnits` as fallback for the ~14 % of days without HealthKit. Loop's commanded `rate × duration` is never used (overcounts ~1.7×). See [`docs/tdd_calculation.md`](../docs/tdd_calculation.md).
- **Bolus/basal dedup** moved from exact-timestamp to **nearest-minute** + value. Catches both BDDP re-ingests (~14,000× observed) and Loop's intermittent dual-sync ~2.5 s / ~15 s pairs that the exact key missed. Same key used in `export_user_day_bolus_counts.py` and `export_user_day_tdd.py`.
- **Classification (§7.2)**: simpler BE definition (count all `subType='normal'` boluses; no meal-vs-non-meal split). Arm flags: `in_ce0_be0`, `in_ce0_be_le1`, `in_ce0_be_inf`, `in_ce_gt0`. See [`docs/day_type_classification.md`](../docs/day_type_classification.md).
- **Analysis-ready table**: anchors on classification, JOINs loop_recommendations, LEFT JOINs endpoints/tdd/age. Applies the PLN-1001 cohort filter inline — version known → `version_int < 3_004_000`; version NULL → `local_day < 2024-07-13` (Loop 3.4.0 release; matches FDA `MAX_SEG2_END_DATE`). Computes §7.5 TDD reference (`mean_tdd_user`, `median_tdd_user`, `n_eligible_days_for_tdd`, `tdd_ratio`) over `day_eligible` days. `delivery_strategy` (§7.3) via inline CASE on `dd_autobolus_count >= 3`. See [`docs/tdd_reference_choice.md`](../docs/tdd_reference_choice.md).
- **Age (§7.6)**: DOB from `dev.default.bddp_user_dates`; `age_years` and `is_pediatric` per day; carried through the analysis-ready table. Cohort split itself is applied by the analysis. See [`docs/pediatric_split.md`](../docs/pediatric_split.md).
- **analysis/**: `analysis_8-1` implements §8.1 Method A (per-user paired): Shapiro + paired-t + Wilcoxon + cluster-bootstrap median CI. Outputs `method_a_contrasts.csv`, `method_a_panel_a.png`, `method_a_panel_b_{central,lows,highs}.png` (delta histograms stacked by classification), `figure_8_1a_stacked_bars.png` (4-arm time-in-range). Reuses FDA `analysis/utils/statistics.py` unmodified.
- **exploratory/**: `test_bolus.sql`, `tdd_explore.sql` (TDD data structure + dual-stream + dedup mechanism investigation), `tdd_distribution.sql` (per-day + per-user TDD distribution + dedup-option comparison).
- **Table naming**: all output tables under `dev.fda_510k_rwd.nma_*` (renamed from a transitional `nma2_*` namespace during the iteration). Prior-scaffold `testing/` and `nma_pipeline.yml` still reference the old script names and column shapes and have not been rewired.
- **Open**: §8.1 Method B (LMM), §8.2 day-type × delivery-strategy interaction, §8.3 TDD stratification, pediatric/adult split in `analysis_8-1`, rolling-30-day TDD reference (sensitivity), residual high-TDD outlier follow-up.

## 2026-05-30 — §8.1 completed (Method B + Tables 8.1a/b/c + Figures 8.1b/c + cohort split)

- **Restored `analysis/utils/statistics.py`** (+ empty `__init__.py`) — the shared stats module deleted in the `nma_*`-rename commit, recovered from the last pre-deletion version. Exposes `cluster_bootstrap_ci`, `paired_within_user`, `lmm_arm_contrast` (§8.1 Method B), `lmm_day_strategy_interaction` (§8.2), `lmm_tdd_stratum` (§8.3). Wraps FDA `compute_paired_statistics` by file path. Fix-on-restore: `paired_within_user` now populates `t_stat`/`wilcoxon_stat` from scipy (were hard-coded `np.nan`). `testing/analysis/test_statistics.py` is green again (14 passed).
- **§8.1 Method B** in `analysis_8-1`: `create_table_8_1b` fits `outcome ~ arm + (1|user)` per classification × endpoint. Arm is an ordered Categorical with reference `CE>0`, so the coefficient is **NMA − CE>0** (asserted via the term name). Degenerate / non-converging slices (e.g. near-constant `tbr_very_low`, `tar_very_high`) are guarded (skip if <2 users/arm or constant outcome) and emit a `converged=False` NaN row instead of raising. Adds the §8.1 Method B non-parametric companion: per-user within-arm **median**, paired NMA−CE>0, with `cluster_bootstrap_ci`.
- **Tables 8.1a / 8.1c**: `create_table_8_1a` (per-arm across-user mean ± SD of per-user means + user-day / contributing-user count rows); `create_table_8_1c` (CE>0-day behavioral summary as mean±SD and median[IQR]). **Meal-bolus decision**: no dedicated meal-bolus count exists, so `carb_entry_count` is used as a footnoted proxy; `bolus_entry_count` is manual/correction boluses.
- **CE>0 comparator restriction** (`restrict_comparator`): the CE>0 arm is limited to users with ≥1 CE=0 day (reduces to ≥1 `in_ce0_be_inf` day given nesting). Applied once in `run()` so all contrasts/tables/figures use the restricted comparator.
- **Figures 8.1b / 8.1c**: combined violin+box of per-user mean TIR (8.1b) and time <70 / <54 (8.1c) across the 4 arms; empty/singleton arms guarded for sparse pediatric cohorts. Existing Panel A/B kept as extras.
- **Pediatric/adult split (§7.6)**: `run(cohort="adult"|"pediatric"|"all")` + `main()` orchestrator writing per-cohort subdirs `outputs/analysis_8_1/<cohort>/`. NULL `is_pediatric` excluded from adult/pediatric, retained in `all`. PLN-1001 age floor wired as a dormant `min_age` param (default `None`/off) pending the footnote-[a] decision.
- **Loader refactor**: `prepare_day_level(pdf)` (pure: numeric coercion incl. behavioral/age cols + eligibility filter) split out from the Spark read so the builders are unit-testable on a plain DataFrame.
- **Deferred**: §8.1 analysis tests (`testing/analysis/test_analysis_8_1.py`) remain skipped placeholders (held per request); §8.2 / §8.3 still stubs (their LMM helpers are now ready); rolling-30-day TDD reference; high-TDD outlier follow-up.

## 2026-05-30 — §8.1 weighting sensitivity investigated (LMM vs Method A divergence)

- Ran §8.1 locally off the analysis-ready CSV snapshot and noticed Table 8.1b (LMM, Method B) and Table 8.1a (Method A, per-user paired) **disagree in sign** on the stringent NMA arms (CE=0/BE=0, CE=0/BE≤1) for TIR/TAR/mean glucose; CE=0/BE≤∞ agrees.
- Root cause is a **weighting choice on identical per-user paired differences**, not a bug. A weighting decomposition (no subsampling) shows the random-intercept LMM coefficient equals the **harmonic effective-n (within-user precision) weighting** of the per-user diffs to ~0.06 across all 24 cells; **equal-user weighting = Method A**. Precision weighting upweights heavy-NMA-day users, who have NMA-favorable diffs (corr ≈ +0.10), flipping the sign. (Naive day-pooled OLS without the random intercept agrees with Method A, so it is the RE structure, not "day pooling".) Inclusion ruled out: refit on the 646 paired users only is unchanged.
- Driver: heavy-contributor concentration — top 5% of users hold ~69%/67% of CE=0/BE=0 / BE≤1 day-rows (median 2 days/user), vs 12% for CE>0.
- Conclusion: **CE=0/BE≤∞ robust** (NMA modestly better across all weightings); **stringent arms indeterminate** (sign-fragile, ~−3 to +3 TIR pts) — report no directional claim. CV robustly lower on NMA across all arms. Equal-user-weight (Method A) is the appropriate population characterization per §8.1/§11.
- Findings verified three independent ways (code review, statistical critique, from-scratch reproduction). Artifacts: [`docs/weighting_sensitivity.md`](../docs/weighting_sensitivity.md), [`exploratory/lmm_weighting_sensitivity.py`](../exploratory/lmm_weighting_sensitivity.py), `analysis/outputs/analysis_8_1/supplement/methodA_weighting_decomposition.csv` (relocated from `all/` 2026-05-31 — see that entry).

## 2026-05-31 — §8.1 age-stratified run produced + Sample Information (Table 1) + gender wired

- **Age-stratified §8.1 run produced.** The adult/pediatric/all split was already wired in `analysis_8-1` but only `all/` had ever been generated. Ran `main()` locally off the CSV snapshot → `outputs/analysis_8_1/{adult,pediatric,all}/` now all populated. Eligible cohort sizes: **adult 1,753 users / 697,281 user-days**, **pediatric 516 / 196,169**, **all 2,214 / 893,475**. (No code change to the split — pure execution gap.)
- **Sample Information (Table 1)** — new `create_sample_information(pdf, cohort)` in `analysis_8-1`, emitted per cohort as `sample_information.csv` (rows: users n, user-days n, age mean±SD / median[IQR] / min–max, age composition, sex %). `main()` concatenates the three into `outputs/analysis_8_1/table_8_1_sample_information.csv` (adult/pediatric/all columns). Per-user grain: age at **first eligible day**, sex per-user-constant. Added `_bin_sex` (mirrors FDA `analysis_8-5._bin_gender`) + `SEX_CATEGORIES`, and a shared `_here()` path helper.
- **Cross-over reconciliation:** standalone adult (1,753) > the `all`-column adult composition (1,697) by exactly the 56 cross-over users (pediatric on their first eligible day, adult on later days). 1,697 adult + 516 pediatric + 1 unknown-DOB = 2,214 = `all`. Documented in [`docs/pediatric_split.md`](../docs/pediatric_split.md).
- **Gender added to staging.** `export_user_day_analysis_ready.py` now LEFT JOINs `dev.default.user_gender` (`g.gender`, key `cls._userId = g.userid`) — same source/pattern as FDA `export_valid_transition_segments.py`. **Snapshot not yet regenerated**, so sex rows currently read `N/A (gender pending)`; Sample Information degrades gracefully (guarded on `"gender" in columns`, like the statsmodels guard in Table 8.1b). Re-run the Databricks staging → re-run `analysis_8-1` locally to populate sex.
- **Data-quality finding (Age):** the min–max row surfaced bad source DOBs — **1 eligible user at ~914 yr** (corrupt DOB; inflated adult mean/SD: 39.3 ± 24.7 vs SD ≈ 13 without it) and **43 users < 6 yr** (below PLN-1001 `MIN_AGE=6`).
- **Age gating implemented (two-sided), resolving footnote [a]:**
  - *Upper bound at extraction* — `export_user_day_age.py` nulls any computed age `> MAX_PLAUSIBLE_AGE (120)` or negative (corrupt DOB), treating it as unknown DOB. **Applied** — the snapshot was regenerated same-day (the Databricks `nma_user_day_age` → analysis-ready chain re-run) and the analysis re-run: adult age max 912.2 → **95.9**, adult mean/SD 39.3 ± 24.7 → **38.8 ± 13.2**, adult users 1,753 → 1,752, unknown-age users 1 → 2 (the corrupt-DOB user retained in `all`). The missing-sex age SD also normalized (45.3 → 13.4).
  - *Lower bound in analysis* — `filter_cohort(min_age=MIN_AGE=6)` is now the **default** in `run()`/`main()` (was a dormant `None` hook). Drops users known <6, retains unknown/nulled-age users (PLN-1001 `is_age_eligible OR dob IS NULL`); `min_age=None` disables.
- **Gender snapshot regenerated + re-run.** With `gender` now in the snapshot and the age floor on, the §8.1 outputs were regenerated. Sample Information now reports sex (overall ~40% M / 34% F / **26% Other/Unknown** — flag the missingness) and the min-age effect (pediatric 516→**473** users, min age 0.1→6.0; 1 null-DOB user retained in `all` as Unknown age; 1,697 adult + 473 pediatric + 1 unknown = 2,171).
- **Sex-missingness sensitivity** — new `create_sex_missingness_sensitivity` (FDA §8.5 `run_sensitivity_gender_missing` analog), emitted per cohort as `sex_missingness_sensitivity.csv`: recorded-vs-missing-sex users compared on age, mean TIR, mean time <70, eligible-day count (Welch t). Finding: missing-sex users contribute **far fewer eligible days** (all: 322 vs 434, p≈6e-18; pediatric similar) and have **marginally lower TIR** (74.9 vs 73.4, p=0.02); age and time-<70 don't differ → sex split broadly representative on glycemic outcomes, missingness tracks engagement. ("Missing" = null/blank raw gender, per FDA; recorded 1,620 + missing 551 = 2,171, the 6-user gap vs M+F being non-M/F codes binned Other/Unknown.)
- **Secondary objective bullet 3 (NMA-day-type frequency)** — new `create_nma_day_frequency` in `analysis_8-1`, emitted per cohort as `nma_day_frequency.csv`: per nested classification, users contributing ≥1 such day (n, % of cohort) + per-user day-count distribution among contributors (mean ± SD, median [IQR], max). Implemented here (sibling of Table 8.1c, the other secondary-objective descriptive) rather than reviving the prior-scaffold `data_overview.py` stub. Finding (all cohort): 30.1% of users contribute ≥1 CE=0/BE=0 day, 32.4% CE=0/BE≤1, 62.6% CE=0/BE≤∞; heavy right skew (median 2 days/user, mean 11.9, max 553) corroborates the heavy-contributor concentration behind the §8.1 weighting caveat. (§4 secondary-objective bullet 4 — behavior on bolus days — was already covered by Table 8.1c.)
- **Output hygiene:** `run()` now clears its per-cohort dir (`shutil.rmtree` + recreate) before writing, so `outputs/analysis_8_1/<cohort>/` reflects only the current run (no stale files from renamed/removed outputs). Only the named cohort dir is wiped — the parent-level combined Table 1 and the sibling `supplement/` dir are left intact. The exploratory weighting-sensitivity artifacts (`methodA_weighting_decomposition.csv`, `overrepresented_users.csv`, `figure_nma_days_vs_tir.png`, plus the manual `method_comparison.csv`) were moved `all/` → `supplement/`, and `exploratory/lmm_weighting_sensitivity.py` now writes there (it still reads `table_8_1b` from `all/`).

## 2026-05-31 — §8.2 wired (day-type × delivery-strategy interaction) + shared `utils/data_loader.py`

- **Shared `analysis/utils/data_loader.py` extracted** (per MJC: share the loader rather than duplicate). Moved the analysis-ready snapshot loader (`prepare_day_level`, `load_day_level`, `default_analysis_ready_csv`, `analysis_dir`), the §7.6 `filter_cohort`, the §8.1 `restrict_comparator`, the by-path statistics loaders (`load_fda_statistics`, `load_nma_statistics`), and the shared constants (`ENDPOINTS`, `CLASSIFICATIONS`, `COMPARATOR_FLAG/LABEL`, `FIGURE_ARMS`, `MIN_AGE`, `NUMERIC_COLS`) out of `analysis_8-1`. §8.1-specific bits (`BEHAVIORAL`, `SEX_CATEGORIES`, `BOOTSTRAP_*`, all table/figure/sample-info builders) stay in `analysis_8-1`.
- **§8.1 refactored to consume `utils.data_loader`** (replace-in-place; no back-compat shims). Verified **behavior-preserving** by regenerating all three cohorts off the snapshot: standalone cohort sizes + age stats unchanged (adult 1,752 users / age 38.8 ± 13.2; pediatric 473; all 2,171). Both scripts add a small `sys.path` bootstrap so `from utils.data_loader import …` resolves regardless of launch dir (Databricks fallback retained); the local CSV (no-Spark) path is what was verified.
- **§8.2 implemented** in `analysis_8-2_nma_by_delivery_strategy.py` (was a Phase-A stub), mirroring §8.1's per-cohort `run()`/`main()` + output-clearing. Per nested classification, fits the day-level interaction LMM `outcome ~ day_type * delivery_strategy + (1|user)` via `utils/statistics.lmm_day_strategy_interaction` — `day_type` derived as the classification's NMA days vs the CE>0 comparator; `delivery_strategy` ∈ {autobolus_on, temp_basal_only} (ambiguous excluded, §7.3); CE>0 comparator restricted as in §8.1. Reference levels (alphabetical): main day_type = NMA−CE>0, main strategy = temp_basal_only−autobolus_on, interaction = NMA−CE>0 contrast shift on TB vs AB days.
- **Outputs** (`outputs/analysis_8_2/<cohort>/`): `table_8_2b_interaction.csv` (per classification × endpoint: main day-type/strategy + interaction coef/CI/p, n_users/n_days, `converged`), `table_8_2a_marginal_cells.csv` (per classification × endpoint × day_type × strategy: observed mean±SD + model-estimated marginal mean), and `figure_8_2a..d` (per-user TIR / time<70 violin+box by cell; model interaction plot; stacked glycemic-range bars).
- **Sparse-autobolus finding / guard:** `autobolus_on` is only ~2% of user-days (22,097 / 948,814), so stringent NMA × autobolus_on cells are tiny → some (classification, endpoint) fits are degenerate/singular. These are guarded (require both day-type levels + both strategies, ≥2 users in each of the 4 cells, non-constant outcome; LMM wrapped in try/except) and emitted as `converged=False` NaN rows rather than aborting the table (mirrors §8.1 `create_table_8_1b`). E.g. pediatric CE=0/BE=0 TIR is not fittable; CE=0/BE≤1 and CE=0/BE≤∞ fit (TIR interaction CE=0/BE≤∞ = −2.46 [−6.70, 1.79], p=0.26 — no evidence the NMA effect differs by strategy in pediatric).
- **Prior-turn cleanup:** an earlier session acted on hallucinated file reads (invented `run_8-*.py`/`main.py`); removed the two bogus files it created (`analysis/analysis_8-2.py`, `analysis/run_8-2.py`), restored the tracked `analysis/outputs/.gitkeep`, and regenerated the deleted (git-ignored) §8.1 results dir. (A stray case-variant `outputs/analysis_8_1/ADULT/` from an old run remains; git-ignored, left in place.)
- **Remaining:** §8.3 (TDD stratification) still a stub; the §8.2 figures replicate plotting locally — a future pass could share §8.1's violin/stacked-bar helpers if §8.3 wants them too.

## 2026-06-01 — Autobolus reclassification: central bolus classifier wired into BE + delivery_strategy

Investigation (`exploratory/autobolus_as_normal_bolus.py`, `autobolus_hk_vs_dd_gap.sql`) found that Loop records autoboluses as `type='bolus'`, `subType='normal'` — indistinguishable from manual boluses by subType (~43% of all Loop boluses are automatic, all 'normal'). Two consequences on the old pipeline: BE (`subType='normal'`) silently counted autoboluses, so CE=0 autobolus days were never BE=0 — the CE=0/BE=0 and CE=0/BE≤1 arms excluded all autobolus users; and `delivery_strategy` (dd-only, `dd_autobolus_count >= 3`) missed ~97% of HealthKit-tagged autobolus days, labeling ~2% of days `autobolus_on` when the true in-cohort rate is ~72%.

- **Detection signals:** HealthKit `MetadataKeyAutomaticallyIssued` flag (authoritative, but absent on ~50% of normal boluses — "HK-silent") + a dosingDecision fallback (loop-DD prior 5s, no `normalBolus` DD ±15s). §5 of the exploration sized the fallback: 3.56M (7.46%) of normal boluses are HK-silent + dd-automatic, recovered at only ~0.11% conflict with explicit HK-manual — so the fallback is warranted (HK alone is insufficient).
- **New staging script `export_user_day_bolus_classification.py`** → `nma_user_day_bolus_classification` (per user-day: manual / automatic counts + hk/dd breakdown). HK-first / dd-fallback rule; deduped on (user, nearest-min, units) with the automatic signal MAX-aggregated across duplicate (HealthKit + Loop-direct) representations. Single source of truth; closes the pipeline-wide blind spot — `subType='normal'` + HK-silent + dd-automatic boluses are counted by neither `dd_autobolus_count` (needs subType≠'normal') nor `hk_autobolus_count` (needs the flag).
- **Wired (NMA first; FDA later):** BE = `manual_normal_bolus_count` — `export_user_day_bolus_counts.py` now projects the classifier (no longer reads BDDP; supersedes the interim HK-only exclusion). `delivery_strategy` = `automatic_bolus_count >= 3` in `export_user_day_analysis_ready.py` (replaces the interim `GREATEST(dd,hk)`); analysis-ready now carries `automatic_bolus_count` / `auto_hk_count` / `auto_dd_count` instead of `dd_/hk_autobolus_count`. DAG updated (`nma_pipeline.yml`: classifier upstream of bolus_counts + analysis_ready; phantom `export_user_day_strategy` dropped).
- **Docs:** new `docs/manual_bolus_identification.md` + `docs/dosing_strategy_classification.md` + `docs/carb_entry_identification.md` (stub); corrected the stale autobolus-subType claim in `docs/day_type_classification.md`; synced `architecture.md`.
- **Pending:** not yet re-run on Databricks — re-run `export_user_day_bolus_classification` → `export_user_day_bolus_counts` → `export_user_day_classification` → `export_user_day_analysis_ready` (fresh snapshot) → re-run §8.1/§8.2. **§8.1 stringent arms and §8.2 outputs are superseded until then.** `exploratory/autobolus_hk_vs_dd_gap.sql` reads the now-removed `ar.dd_autobolus_count` (will break; spent artifact). `nma_pipeline.yml` still has other prior-scaffold phantom task names (broader reconciliation pending). FDA-pipeline wiring + test deferred.

## 2026-06-01 — §8.3 wired + finding-explanation supplement + weighting supplement retired

Explains the counterintuitive §8.1 result (TIR higher on CE=0 / no-meal-announcement days). Structural reframe: CE=0 ⟺ `carb_grams_total = 0` exactly, so a CE=0 day is the zero endpoint of the announced-carb axis → leading explanation is an **intake confound**, not "disengaged dosing is better." Built on the autobolus-corrected snapshot (the classifier regen has run — snapshot now carries `automatic_bolus_count`/`auto_hk_count`, no `dd_autobolus_count`).

- **New supplement `analysis/analysis_8-supp_nma_finding_explanation.py`** (→ `outputs/analysis_8_supp/<cohort>/`; reuses §8.1 contrast/figure machinery + utils; §8.1 audit trail untouched):
  - S1 intake characterization (CE=0 vs CE>0): pediatric CE=0 days use far less insulin — TDD 32 vs 48 U/day (Δ−15.9), bolus 17 vs 32 (Δ−15.0), basal ~unchanged. CE=0 days are genuinely low-intake.
  - S2 carb dose-response: TIR flat (~66) from 0→90 g, drops at 90+ g; ~70% of CE>0 days are 90+ g, so the CE>0 average is dragged down — CE=0 isn't uniquely good, it's the low-carb end. Within-user slope −0.3 TIR / 10 g (p<0.001).
  - S3 decomposition/safety: aggregate TIR +1.5 = less hyper (TAR −1.9) minus a small hypo cost (TBR +0.36) — benign with a minor time-below caveat.
  - C1–C4 confounder checks (selection bias, CE=0-day clustering, CGM coverage, weighting note).
- **§8.3 wired** (`analysis_8-3_nma_tdd_stratified.py`, was a Phase-A stub): within-user Low vs High TDD on CE=0 days (R = tdd/mean cut at 1.0; eligibility `n_eligible_days_for_tdd ≥ 30` + ≥1 day/stratum). Tables 8.3a/b/c + tercile, median-ref & rolling-30-day sensitivities + Figures 8.3a/b/c/d (incl. violin+box+dots by stratum). **Decisive finding:** Low-TDD CE=0 days TIR ~68 vs High-TDD ~49 (Δ ≈ +13 to +19, p<0.001 across arms) — light-intake days drive the aggregate benefit; High-TDD (likely unannounced-meal) CE=0 days are much WORSE (TAR ~50, mean glucose ~188) than the CE>0 comparator (~64). Rolling-30-day reference is computed in-analysis (trailing 30-calendar-day mean of per-day tdd_units; no staging column needed) and agrees with the mean-ref result (TIR Low−High within ~1 pt). Caveat: winsorize residual high-TDD outliers before strong High-stratum claims.
- **Weighting-sensitivity supplement retired** (the two §8.1 methods are expected to agree post-fix, since the stringent arms now include autobolus users): `docs/weighting_sensitivity.md` carries a SUPERSEDED banner and is kept for historical context; `exploratory/lmm_weighting_sensitivity.py` + `outputs/.../supplement/` are no longer a deliverable.
- Mirrors §8.1/§8.2 per-cohort `run()`/`main()` + output-clearing; runs locally off the snapshot (verified pediatric; all-cohort outputs generated). Pending: unit tests (`testing/analysis/test_analysis_8_3.py` + a supplement test) remain to write; cited numbers are the pediatric smoke-test.

## 2026-06-01 — Figure conventions unified across §8.1/§8.2/§8.3 (+ supplement) into `utils/plotting.py`

Brought every analysis's plots onto the §8.3 vocabulary so the report reads consistently. New shared module **`analysis/utils/plotting.py`** owns: range-based `ENDPOINT_COLORS` (TIR green, <70/<54 coral/red, >180/>250 light/dark purple; mean glucose / CV / hypo events = Tidepool brand blue), `RANGE_COLORS`/`RANGE_COLS`, the two 2×2 metric `GRIDS` (Grid 1 target+safety = TIR/<70/<54/hypo; Grid 2 hyper+overall = >180/>250/mean/CV), `violin_box_panel` (subtle dots **behind**, crisp box + orange median **on top**, optional treatment|comparator separator) and `overlay_hist_panel` (one set of shared bin edges so overlaid bars align, solid mean line + dashed zero). Convention: the treatment arm (NMA / CE=0) carries the endpoint colour, the CE>0 comparator is grey.

- **§8.3** refactored onto the shared helpers (removed its local colour/grid constants + inline violin/hist code); outputs unchanged. Earlier this session: dots de-intensified + box drawn on top; histogram bins aligned via shared edges.
- **§8.1**: per-user figures are now the two 2×2 violin grids (all 8 endpoints × the 4 arms — 3 nested NMA graded light→dark by breadth + grey CE>0) as `figure_8_1b_violin_grid{1,2}_*.png`, and the paired-difference (NMA−CE>0, broadest arm) histogram grids as `figure_8_1c_paired_delta_grid{1,2}_*.png`. **Removed** `make_panel_a`/`make_panel_b` (+ `method_a_panel_*.png`), the TIR-only `figure_8_1b` and TBR-only `figure_8_1c`, and the local `CLS_COLORS`/`ZERO_LINE_COLOR`/`ENDPOINT_GROUPS`/`RANGE_*`. `figure_8_1a_stacked_bars.png` kept (shared palette). Exposed `arm_violin_groups()` for reuse.
- **§8.2**: per-user figures are the two 2×2 violin grids (strategy × day-type cells, all 8) as `figure_8_2a_violin_grid{1,2}_*.png`; the interaction marginal-means plot is now all-8 grids `figure_8_2c_interaction_grid{1,2}_*.png` (was TIR-only); `figure_8_2d_stacked_bars.png` kept. **Removed** the TIR/TBR-only `figure_8_2a`/`figure_8_2b` + `_violin_box_*`/`_cell_per_user_means` + local `DAY_TYPE_COLORS`/`RANGE_*`. The per-endpoint figures use the broadest classification (CE=0/BE≤∞); stricter-arm interaction coefficients stay in Table 8.2b and 8.2d keeps all three classifications.
- **Supplement** rewired off the removed §8.1 helpers: `figure_s1_intake` now uses `violin_box_panel` + `a81.arm_violin_groups` (TDD/bolus aren't glycemic ranges → Tidepool brand), and the S3 TBR figure is a new local `figure_s3_tbr` (replaces the retired `a81.make_figure_8_1c`).
- Renames the per-user figure files — **flag for the report editor** (figure *content* is preserved: TIR/TBR now live in Grid 1 of the violin grids). All four modules `py_compile` clean and regenerate adult/pediatric/all without error; spot-checked the new grids visually.
- **Follow-up (same day): bigger fonts across all figures.** `utils/plotting.py` now sets a house-style `rcParams` font bump (applies to every NMA figure on import) + exports font-size constants (SUPTITLE_FS/TITLE_FS/TICK_FS/LABEL_FS/LEGEND_FS) used by the panel helpers + each analysis's suptitles/annotations. (P-values were briefly added to the violin titles and then removed at MJC's request — the figures carry no p-value annotations.) All cohorts regenerated.

## 2026-06-02 — Carb-entry (CE) detection coverage validated (food-only is sufficient)

Ran the carb analogue of the bolus-classifier investigation — could carb entries hide under another `type`/JSON path (under-detection → real meals leaking into the NMA arm) or could spurious rows inflate CE? New exploration script [`exploratory/carb_detection_coverage.sql`](../exploratory/carb_detection_coverage.sql) (schema discovery → food JSON coverage → wizard ruling-out → dosingDecision COB → 0-gram → dedup → arm impact). Findings (BDDP `dev.default.bddp_sample_all_2`):

- **Only `food` and `wizard` carry carbs.** `food` 31.15M rows, **99.9% with `nutrition.carbohydrate.net`** (uniform `{"carbohydrate":{"net":N,"units":"grams"}}`, ~27k/0.09% dropped). `food.origin` is null → no automatic/app-generated-carb flag (the autobolus analogue does NOT exist for carbs).
- **`wizard` ruled out.** ~40M carb-bearing wizard rows in BDDP overall, but they touch only **493 of 1,027,382 analyzed Loop user-days (0.048%)**, are non-Loop (`loop_version` null), and carry no linked `food` — bolus-calculator entries from non-Loop pumps/AIDs, a separate population, not missed Loop meals.
- **0-gram entries immaterial:** only **8/926,553 CE>0 days (0.001%)** are CE>0 solely from 0-gram logs.
- **Arm membership robust to every rule (§8b):** of 1,027,382 analyzed days the NMA arm = **100,829 CE=0 days**; identical counting any food row, −98 (0.097%) if wizard counted, +8 (0.008%) if 0-gram dropped — i.e. no carb-detection choice moves the arm by >0.1%.
- **Decision:** `food`-only carb detection (CE = food-entry count; CE=0 ⟺ `carb_entry_count = 0`) is correct and complete for the DIY-Loop cohort. **No reclassification needed** (unlike boluses). Non-blocking open item: the dedup key uses exact `time_string` (not nearest-minute) and keeps same-timestamp edits — can only affect `carb_entry_count` *magnitude* (§7.5 metric), never CE=0/CE>0 arm membership; §7 of the script audits it. Findings + decisions written into [docs/carb_entry_identification.md](../docs/carb_entry_identification.md) (no longer a stub).

## 2026-06-04 — §8.3 supplemental terciles + figure 8.3e; integration-test harness implemented; ⚠️ TDD/tercile caveat

**§8.3 supplemental + scatter.**
- Added a supplemental Low/Mid/High **R-tercile** Table 8.3a (`table_8_3a_supp_terciles.csv`): `_tercile_strata` now labels every CE=0 day Low/Mid/High by the user's own R terciles and returns all three (the binary Low/High contrast in `table_8_3b` just reads the Low/High strata, Mid ignored), and `table_8_3a_per_user_by_stratum(strata_order=...)` parametrizes which strata to report. (Earlier draft used a `keep_mid` flag; simplified to "caller takes what it needs" per MJC.)
- New **`figure_8_3e_tir_vs_tdd_percentile.png`**: per-day TIR vs each day's within-user TDD percentile (rank over ALL eligible days, CE=0 + CE>0), coloured by CE/BE category (CE=0 BE=0/1/≥2 on a green→amber→red ramp + CE>0 grey), per-category decile-mean lines (11 dots on the 0/10/…/100 ticks) + a dashed black overall-mean line.

**⚠️ TDD / tercile results need real understanding before citing** — diagnosed two issues:
- The empirical tercile rule (`tdd_ratio <= q1` Low / `>= q2` High / strict-interior Mid) splits *days* ~evenly but covers *unequal user sets* (adult CE=0/BE=0: Low 879 / Mid 477 / High 611). It's Low-biased (`<= q1` inclusive, checked first) and degenerate for users with few/clustered CE=0-day TDD (1 CE=0 day → Low only; 2 → Low+High; ties can empty Mid at any day count — confirmed with toy cases). So the user-weighted Low/Mid/High means aren't apples-to-apples.
- TIR is a band metric → Low≈Mid (reclaimed hypo ≈ added hyper) while mean glucose / TAR are monotonic, which is why the tercile table looks at odds with figure_8_3e's slope (figure is day-pooled over all days incl. CE>0; table is per-user CE=0 terciles).
- Candidate fixes (NOT yet applied): same-user-set gate (only users with all 3 strata) + rank/qcut balancing; and/or a parametric mean±SD split (±1 SD ≈ 16/68/16 tails-vs-middle; ±0.43 SD = parametric terciles; centre = CE=0-day mean of R, < 1).

**Integration-test harness implemented** (`testing/integration/`):
- `run_pipeline.py` (was a stub naming deleted modules) now builds the synthetic BDDP fixture, seeds the FDA upstream tables it reads (loop_recommendations via `make_loop_recs`; loop_cbg built directly — FDA's `export_cbg_from_loop` reads glucose from `value`, which the fixture left null), runs the 9 current staging modules → `dev.fda_510k_rwd.test_nma_*` (terminal `…analysis_ready`), and exports a CSV fixture. Idempotent; `get_spark()` mirrors FDA's Databricks-Connect pattern. No RedirectingSpark (staging + §8.1 parametrize table names).
- Fixture extended for the 2026-06-01 pipeline: autoboluses carry the HealthKit AutomaticallyIssued payload flag (subType='normal'); basal/bolus carry a Loop source origin + a `rate` column (added to the NMA BDDP schema); `normal` typed string (the classifier does `get_json_object` on it); new `nma_user_ce_pos_only` archetype (comparator-restriction probe); new `build_loop_recommendations` / `build_loop_cbg` / `build_user_gender`.
- `run_pipeline.load_analysis_module` imports the hyphenated analysis modules by path, blanking the Databricks notebook preamble — the `%pip install …` magic (a SyntaxError on import) AND a top-level `dbutils.…` line (otherwise sits before `from __future__` → import error) — and injects a no-op `dbutils`.
- Per-analysis checks are **runnable files** (`run_test_analysis_8_{1,2,3}.py`, not `test_*`, so Databricks runs them as files not pytest). §8.1 implemented: `main()` builds the pipeline then asserts paired_diff design recovery + comparator restriction + cohort split + all artifacts; `_ensure_statsmodels()` pip-installs statsmodels on the driver if absent so Table 8.1b's LMM populates. 8.2/8.3 are stubs. The interim pytest per-analysis files + `conftest.py` + standalone drivers (`run_inspection`/`run_end_to_end`/`test_pipeline_end_to_end`/`assertions_8_1`) were consolidated/removed; the `testing/analysis/test_analysis_8_{1,2,3}.py` copies were deleted.
- `exploratory/import_test.py` — Databricks check that statsmodels imports AND fits a MixedLM (§8.1 Method B prerequisite).
- A `%pip install statsmodels` + `dbutils.library.restartPython()` preamble was added to the analysis files for direct-notebook runs (commented out in `analysis_8-3`); the loader strips it for imports.

## 2026-06-04 — §8.1 windowed-comparator sensitivity + `_userId` pseudonymization at export (team-review changes)

First tranche of the post-review changes.

- **§8.1 windowed-comparator sensitivity (D15).** Recomputes the NMA-vs-CE>0 contrast with a per-NMA-day ±45-day (90-day) temporal match: each CE=0 day is paired only against the mean of that user's CE>0 days within ±45 calendar days, controlling within-user temporal drift; per-user windowed Δ summarized equal-user-weight (Method A). New `create_windowed_contrast_table` → `table_12_1a_windowed_sensitivity.csv` (3 arms × 8 endpoints, `diff_win` + `diff_full` on the same matched users + paired-t/Wilcoxon + coverage); windowed figures (`make_windowed_stacked_bar` / `make_windowed_violin_grids` / `make_windowed_delta_grids`) → **Appendix §12.1** figures 12.1a / 12.1b / 12.1c (stacked / violins / Δ-histograms). The pooled-within-user full-record §8.1 analysis (8.1a/b/c + `table_8_1a_expanded`) is **unchanged**. Regenerated adult/pediatric/all. Headline (all): broadest-arm TIR Δwin +0.80 (full +0.61); stringent CE=0/BE=0 +3.30.
- **Shared windowing helper.** `utils/data_loader.py` gains `windowed_matched_means(pdf, nma_flag, cmp_flag, endpoints, half)` (per-NMA-day match via per-user cumulative-sum + searchsorted) + `WINDOW_DAYS=90`/`WINDOW_HALF=45`; also **hoisted `STRATEGIES`/`STRATEGY_COL`** out of `analysis_8-2` into `data_loader` (shared with §8.2 and future §8.3; D9). §8.2 now imports them.
- **Verified three ways** — embedded brute-force self-check, exact reproduction of the independent coverage sweep (56,629 broadest-arm matched days), and a blind from-scratch re-implementation (exact to 4 dp).
- **Selection caveat quantified.** Broadest arm matches only 70% of CE=0 days; the 30% unmatched are 72.3% sustained-non-announcing + 27.2% pure non-announcers (users who correction-bolus but never log carbs), with negligible (<1%) coverage gaps — diagnostic saved to `outputs/review_feasibility/unmatched_ce0_day_reasons.csv`. So the window characterizes NMA days during mixed-behaviour periods; the full-record analysis retains the deep non-announcing days. (A stricter CE+BE≥3 "high meal-announcement" comparator was prototyped this session then **dropped** — CE>0 is the sole comparator.)
- **`_userId` pseudonymized at export (D16).** `export_user_day_analysis_ready.py` now hashes `_userId` (deterministic salted SHA-256, `concat('u', substr(sha2(concat(_userId, USERID_SALT),256),1,16))`) at the final SELECT, so the analysis-ready table + CSV snapshot carry an opaque key and the raw id never reaches local disk; the column name is kept so analyses are unchanged. Snapshot regenerated (local `_userId` e.g. `u0ba380071334927b`). Raw id remains in upstream staging for traceback.
- **D7 resolved.** Confirmed the snapshot carries `automatic_bolus_count` + classifier-derived `delivery_strategy` (~77% autobolus_on / 23% temp_basal_only eligible days); stringent-arm / §8.2 results are citable.

## 2026-06-04 — §8.1 high-engagement arm (CE≥3/BE≥3) (team-review changes, D17)

- **New 5th day classification `in_ce_ge3_be_ge3`** = (`carb_entry_count >= 3` AND `bolus_entry_count >= 3`) — "high engagement" / heavy meal-announcement days — staged in `export_user_day_classification.py` and carried through `export_user_day_analysis_ready.py`. Snapshot regenerated on Databricks (carries the staged flag); the interim analysis-side derive bridge in `data_loader.prepare_day_level` was removed.
- **Shown beside the 3 NMA arms + CE>0** as the engagement spectrum's heavy end: Table 8.1a gains a 5th **column**; fig 8.1a a 5th **bar**; fig 8.1b a 5th **violin** (bronze). `data_loader` gains `HIGH_MA_FLAG`/`HIGH_MA_LABEL`/`SUPPLEMENT_ARMS`; `create_table_8_1a` + `make_stacked_bar` iterate `SUPPLEMENT_ARMS`; `arm_violin_groups` gained a gated `include_high_ma` (off by default so §8-supp's reuse stays 4-arm).
- **Parallel supplement contrast tables**: `table_12_1b_high_engagement_lmm.csv` (LMM) + `table_12_1c_high_engagement_windowed.csv` (windowed), CE≥3/BE≥3 vs **CE>0** (overlapping reference kept per MJC, since CE≥3/BE≥3 ⊂ CE>0). `create_table_8_1b` / `create_windowed_contrast_table` parametrized with `classifications=`.
- **Figures 8.1c (full-record) + 12.1c (windowed)** reworked to the NMA-vs-high-engagement comparison: overlay the two NMA-anchored deltas **NMA−CE>0** and **NMA−CE≥3/BE≥3**. The windowed companion figures were **renumbered to mirror the main figure order** — **12.1a** windowed stacked bar (new), **12.1b** windowed violins, **12.1c** windowed Δ-histograms (⟷ main 8.1a/8.1b/8.1c) — and all carry the CE≥3/BE≥3 arm (5 arms / overlay).
- **Table file reorg (doc-editor flag):** `method_a_contrasts.csv` → **`table_8_1a_expanded.csv`** (the inferential Method A expansion of Table 8.1a) so every table uses the `table_8_1*` prefix; plan-locked Tables 8.1a/b/c are unchanged. The full Figure↔Table correspondence + name-change list is recorded under todo.md "Flag for the report editor".
- **Restricted to CE=0-contributing users** — `restrict_comparator` now zeros `in_ce_ge3_be_ge3` alongside `in_ce_gt0`, so all arms describe the same cohort (CE≥3/BE≥3 1,290 users, matching CE>0's 1,324-user cohort).
- All 3 cohorts regenerated. **Finding:** high-engagement days modestly worse than typical CE>0 (LMM TIR −0.9 / windowed −1.1); NMA days run ~+1.6 TIR above high-engagement days (fig 8.1c).

## 2026-06-05 — High meal-announcement (CE>=3/BE>=3) arm propagated through §8.2 + §8.3 (D18)

Carried the D17 high-engagement / "HMA" arm (`in_ce_ge3_be_ge3`) into §8.2 and §8.3 so they parallel §8.1's treatment. Scope: §8.2 + §8.3 (§8-supp deliberately left out — exploratory/non-prespecified). MJC: "propagate the HMA users through the rest of the document, starting with 8.2." See decisions.md **D18**.

- **Shared styling hoisted.** `HIGH_MA_COLOR` (#9c6b30 bronze) + `HIGH_MA_ALPHA` moved from `analysis_8-1` into `utils/plotting.py` so §8.1/§8.2/§8.3 share one definition (replace-in-place, no shim, per conventions); §8.1 now imports them.
- **§8.2** (`analysis_8-2_nma_by_delivery_strategy.py`): HMA shown as a 3rd, overlapping day type (HMA ⊂ CE>0, bronze) beside NMA and CE>0 in the descriptive figures — 8.2a violins / 8.2c interaction lines / 8.2d stacked bars go 4 → 6 cells per strategy pair. `build_day_type_frame` parametrized by treatment flag/label; new `build_display_frame` (3 day types) + `DISPLAY_CELLS`; `fit_interaction_models` parametrized with `treatments`/`treatment_label`. New **Appendix §12.2** `table_12_2a_high_engagement_interaction.csv` — the day_type ∈ {CE>=3/BE>=3, CE>0} × delivery_strategy interaction (same columns as Table 8.2b; reference = CE>0, so main_day = HMA − CE>0). Thin HMA×autobolus_on cells caught by the existing `converged=False` guards. Also commented out the leftover `%pip`/`dbutils` notebook preamble so the script is locally runnable like §8.1/§8.3.
- **§8.3** (`analysis_8-3_nma_tdd_stratified.py`): HMA days stratified Low/High by within-user TDD via the existing `_ce0_strata(pdf, HIGH_MA_FLAG)` — shown as a 3rd group (bronze) in figures 8.3a (6 violins), 8.3b (3rd Low−High overlay), 8.3c (4th reference bar). New **Appendix §12.3** `table_12_3a_high_engagement_tdd_strata.csv` — within-user Low−High contrast on CE>=3/BE>=3 days (reuses `table_8_3b_within_user`, mirrors Table 8.3b). **Inherits the D12 caveat** (TDD/tercile results not yet citable) — parallel structure only, does not resolve D12.
- Docs updated: decisions.md D18 (+ D17 marked extended), architecture.md, report_editor_note.md, todo.md.
- **Verified:** regenerated adult/pediatric/all for §8.2 + §8.3, all rows converged. §12.2 all-cohort TIR HMA−CE>0 = −0.92; §12.3 all-cohort HMA-day Low−High TIR = +10.8. Figures spot-checked.
- Follow-up (same day): Table 8.3a gained CE>0 + CE≥3/BE≥3 sections (5 sections total, each Low/High; matches figure 8.3a).

## 2026-06-05 — §8.3 sensitivities reorganized into Appendix §12.3 supplement

Reorganized §8.3's scattered sensitivity outputs into a coherent Appendix §12.3 supplement (flat `*_12_3*` names; cf. §8.1 §12.1 / §8.2 §12.2), and featured the two alternative TDD-reference definitions as full mini-analyses. See decisions.md D12 (updated).

- **Three §12.3 blocks**, each reusing the existing builders (`table_8_3a_per_user_by_stratum` / `table_8_3b_within_user` / `table_8_3c_lmm` / `_ce0_strata`):
  - **Median reference** (R = tdd/median_tdd_user): `table_12_3a_median_per_user_by_stratum` (5 sections) + `figure_12_3a_median_grid{1,2}` (6 groups) + `table_12_3b_median_within_user`.
  - **Rolling-30-day mean reference**: `table_12_3c_rolling_per_user_by_stratum` + `figure_12_3c_rolling_grid{1,2}` + `table_12_3d_rolling_within_user`.
  - **HMA arm** (CE≥3/BE≥3, mean ref): `table_12_3e_high_engagement_within_user` (8.3b parallel) + `table_12_3f_high_engagement_lmm` (8.3c parallel — the new "Table 8.3c parallel with HMA").
- `figure_8_3a_violin` parametrized (`fig_id` / `fname_stem` / `ref_note`) so the median & rolling violin variants reuse it; the median/rolling per-user tables + violins carry the same 5 sections / 6 groups as the primary.
- **Dropped the empirical-tercile outputs** (`_tercile_strata`, `table_8_3a_supp_terciles`, `table_8_3b_sens_terciles`) — the degenerate, not-apples-to-apples part of D12 — pending the two-view rank-tercile rework. Renamed the old flat sensitivity files into the §12.3 scheme (`_sens_median_ref` → `_12_3b_median_within_user`, `_sens_rolling30` → `_12_3d_rolling_within_user`, `table_12_3a_high_engagement_tdd_strata` → `_12_3e_high_engagement_within_user`).
- Regenerated adult/pediatric/all; ⚠️ all §12.3 / TDD-stratum outputs remain **not citable** (D12 unchanged). Docs: decisions.md D12 update, architecture.md, report_editor_note.md §7, todo.md.

## 2026-06-05 — §8.3 two-view rank terciles (D12 RESOLVED)

Replaced the dropped degenerate empirical terciles with **same-user-set-gated rank terciles** in two reference views, fixing both D12 defects. See decisions.md **D12 RESOLUTION**.

- **New machinery** (`analysis_8-3_nma_tdd_stratified.py`): `_rank_strata(reference={overall,ce0}, split={binary,tercile})` cuts strata on a balanced within-user TDD **rank** (`rank(pct=True, method="first")` — deterministic tie-break, no RNG); `_same_user_set_gate` keeps only users present in every stratum → **n_users equal across Low/Mid/High** (verified PASS, all cohorts) — fixes D12 issue (1) unequal user sets. `table_8_3a_per_user_by_stratum` regained its `strata_order` param.
- **Overall reference promoted to primary §8.3.** Rank each day's TDD over the user's *all* eligible days (à la fig 8.3e) → cleanly **monotonic** TIR (all 75.6/68.2/57.9; adult 76.6/69.5/59.2; ped 69.8/61.4/50.9) — fixes D12 issue (2) band-insensitivity. Outputs: `table_8_3d_rank_tercile_strata.csv` (5 sections: 3 CE=0 + CE>0 + HMA) + `figure_8_3f_grid{1,2}` (9-group Low/Mid/High violins — `figure_8_3a_violin`/`_violin_panel` generalized to N strata, default Low/High keeps the binary figures unchanged) + `figure_8_3g_grid{1,2}` (the **5 day types** — 3 nested CE=0 + CE>0 + HMA — across Low/Mid/High terciles as **staggered vertical 95% CI bars**, all 8 endpoints; per follow-up request, replacing the initial two-reference trend figure). The CE=0-reference companion of 8.3g is `figure_12_3i_ce0_bars_grid{1,2}` (§12.3).
- **Appendix §12.3g–j** (the rest of the two views): `table_12_3g_ce0_rank_tercile_strata` (CE=0-reference tercile — preserves the band quirk Low≈Mid, so read with mean glucose/TAR) + `figure_12_3g_ce0_grid{1,2}`; `table_12_3h_rank_binary_strata` (both refs' balanced binary Low/High); `table_12_3i/j_rank_within_user_{overall,ce0}` (within-user bottom−top contrasts, binary + tercile); `figure_12_3i_ce0_bars_grid{1,2}` (CE=0-ref companion of fig 8.3g — 5 day types × terciles, staggered CI bars); `figure_12_3h_overall_tercile_scatter` (fig-8.3e with 33/67 tercile bands).
- **High-TDD winsorization — evaluated, then removed (per MJC).** A 300 U/day cap was prototyped (surgically adjusting the staged mean for the capped excess so unaffected users stayed exactly preserved), but it touches only **≈6 eligible days / 5 users (0 CE=0)** and the rank terciles are outlier-robust by construction → immaterial. Removed rather than kept for complexity; the architecture Open-Questions high-TDD note is closed.
- **Finding:** the rigorous within-user **Low−High TIR contrast is +17.6 across all cohorts** (Wilcoxon p≈1e-82; n_pairs 471 adult / 99 ped / 556 all) — High-TDD (likely unannounced-meal) CE=0 days run much worse, corroborating D11's intake-confound framing.
- **Verified:** regenerated adult/pediatric/all; gate invariant PASS; tercile day counts balanced (CE=0-ref ~27.5–28.1k each) / overall-ref correctly Low-heavy (CE=0 days skew low-TDD); all 6 new PNGs/cohort render. **Citation:** rank-tercile outputs **citable** (D12 resolved); magnitude strata (8.3a/b/c, 12.3a–f) superseded. Docs: decisions.md D12 RESOLUTION, CLAUDE.md caveat, architecture.md, report_editor_note.md §7, todo.md.
- **Follow-up figure refinements (same day, MJC):** fig 8.3g switched from the two-reference trend to the **5 day types × terciles as staggered vertical 95% CI bars with connecting lines**, two grids (CE=0-ref companion = fig 12.3i); fig 8.3f (and 12.3g_ce0) 9-group tercile violins re-laid-out **4×1** (wide panels); fig **8.3c → 10 stacked bars** (5 day types × Low/High) with §8.1-style black labeled %s, dashed Low→High segment connectors, and the legend moved to the bottom; fig **8.3e (and its §12.3h tercile-band variant) re-coloured to the same 5 day types as 8.3g** (`DAY_TYPE_COLORS`, shared palette hoisted) — 5 per-day-type decile lines (cumulative flag membership, matching 8.3g) + dots coloured by each day's most-specific day type, all days shown with the two big HMA/CE>0 categories drawn fainter (smaller, on the bottom) as a backdrop so they don't wash out the greens; restricted to the 5-day-type (CE=0-contributing) cohort. The old BE=0/BE=1/BE≥2 green→amber→red split + `CE_BE_COLORS` were dropped. The shared `DAY_TYPE_COLORS` green ramp was **widened for distinctness** (dark `#00441b` → mid = the TIR/70-180 green `endpoint_color("tir")` → light yellow-green `#b7e075`) so the 3 nested CE=0 categories stay tellable apart in the dense scatter — propagated to all §8.3 figures (8.3c/8.3g/12.3g/12.3i/12.3h).
- **Fast figure iteration (perf):** `run()`/`main()` gained `--figures-only` (skip the slow LMM + cluster-bootstrap table writes; leave existing table CSVs intact) and `--figs <tag>` (render only matching figure builders, e.g. `--figs 8_3g`; implies figures_only). `--figs 8_3g --cohort all` ≈ 9 s vs a multi-minute full run. Tags: 8_3a/b/c/d/e/f/g, 12_3a/c/g/h/i.

## 2026-06-06 — Day-type colours + fast figure-gen propagated to §8.1 + §8.2 (MJC)

Carried the §8.3 day-type palette and the fast-render flags into §8.1 and §8.2 so the document reads consistently and all three analyses iterate quickly. See decisions.md **D13 update (2026-06-06)**.

- **`DAY_TYPE_COLORS` hoisted to `utils/plotting.py`** (one shared definition; §8.3 now imports it, replacing its local copy). The arm/day-type figures use this fixed palette (3 nested NMA/CE=0 greens dark/TIR-green/light, CE>0 grey, HMA bronze) instead of D13's per-endpoint arm colour — so a day type reads identically in every panel and across §8.1–§8.3.
- **§8.1:** `arm_violin_groups` + `make_windowed_violin_grids` recoloured — 8.1b + 12.1b violins now use DAY_TYPE_COLORS at a uniform `VIOLIN_ALPHA` (retired `NMA_ARM_ALPHAS`/`COMPARATOR_ALPHA`/the per-endpoint `base`). Unchanged: 8.1a/12.1a stacked (range), 8.1c/12.1c Δ-hist (comparison).
- **§8.2:** `_cell_violin_groups` (8.2a) and `make_interaction_grids` (8.2c) recoloured to DAY_TYPE_COLORS, and **extended to show all 5 day types** (the 3 nested NMA/CE=0 arms + CE>0 + HMA), matching §8.1's 8.1b — 8.2a via a new `build_display_frame_5way` + `DISPLAY_CELLS_5WAY` (10 cells = 5 day types, each day type's **TB|AB pair adjacent** — TB lighter / AB darker via `STRATEGY_ALPHA` — plus a dark mean-connector line + diamonds showing the TB→AB shift), laid out **4×1** (full-width panels, à la 8.3f); 8.2c via one line per nested NMA arm (each from its own per-classification fit) + CE>0 (broadest fit) + HMA. `DISPLAY_CELLS` (3-way) is retained for the 8.2d stacked bars (unchanged, range-coloured). Dropped the `GRAY`/`HIGH_MA_COLOR`/`HIGH_MA_ALPHA` imports (now via DAY_TYPE_COLORS).
- **Strategy display order → TB-first (per MJC):** reversed `STRATEGIES` in `data_loader` to `[(temp_basal_only, TB), (autobolus_on, AB)]` so all §8.2 figures (8.2a cells, 8.2c x-axis, 8.2d cells) *and* tables (8.2a/8.2b/12.2a) read TB→AB consistently. Display-only: the LMM uses `outcome ~ day_type * strategy` with statsmodels' alphabetical reference (AB), so the fitted coefficients (main_strategy_coef = TB − AB) are unchanged; `STRATEGIES` is used nowhere in §8.1/§8.3. Regenerated §8.2 (full run, all cohorts).
- **Fast figure-gen** added to both: `--figures-only` (guard table writes + skip the dir-clear) and `--figs <tag>` (builder-registry filter; implies figures_only) + argparse. §8.1 tags 8_1a/b/c + 12_1a/b/c; §8.2 tags 8_2a/8_2c/8_2d. **§8.2 computes the LMM interaction fit lazily** — only when tables run or 8.2c is being rendered (so `--figs 8_2a` skips it, ≈9 s).
- **Verified:** regenerated §8.1 (10 figs/cohort) + §8.2 (5 figs/cohort) across adult/pediatric/all; spot-checked 8.1b, 12.1b, 8.2a, 8.2c — day types consistently coloured, panel titles still endpoint-coloured. §8.3 figures unchanged (palette values identical post-hoist).

## 2026-06-06 — §8.4 delivery strategy (AB/TB) × TDD stratum × day type + carb-entry-rate (D19)

New **§8.4** module (`analysis_8-4_nma_by_delivery_strategy_stratified.py`), SECONDARY/EXPLORATORY, bundling both `delivery_strategy` objectives from the team-review plan. §8.2 left untouched; §8.3 unchanged in substance (helpers hoisted only). See decisions.md **D19**.

- **Code hoist (pure move, verified):** the §8.3 rank-strata + base-table machinery (`_rank_strata`, `_same_user_set_gate`, `_ce0_strata`, `_per_user_stratum_mean`, `table_8_3a/b/c`, `_rank_sections`, `table_rank_across_user/within_user`, `_stratum_delta`, `_tercile_trend_stats`, the `MIN_REF_DAYS`/`R_CUT`/`RANK_*` constants) moved verbatim to **`analysis/utils/strata.py`**; `analysis_8-3` now imports them. Re-ran §8.3 all-cohort — overall-ref tercile TIR still **75.6/68.2/57.9** (unchanged). Added shared `STRATEGY_ALPHA` (AB darker / TB lighter) to `utils/plotting.py`; §8.2 keeps its own local copy (not modified).
- **Part 1 — glycemic × TDD stratum × strategy.** Per-day-type 2-way interaction LMM `outcome ~ tdd_stratum * delivery_strategy + (1|user)` (reuses `lmm_day_strategy_interaction` with `day_type_col="tdd_stratum"`). Headline day type **CE=0/BE≤1 vs CE>0**, **rank-binary** Low/High (overall ref), **composite same-user-set gate** (users present in every stratum×strategy cell). Main: `table_8_4a_strategy_cross_binary` (Method-A cross-tab) + `table_8_4b_strategy_interaction` (Method-B; stratum_main = Low−High, strategy_main = TB−AB, interaction = AB−TB-gap shift Low vs High) + `figure_8_4a_grid{1,2}` (staggered 95% CI bars, colour = day type, alpha = strategy). Thin High-TDD×TB guarded → `converged=False` (n surfaced).
- **Part 2 — carb-entry-rate by strategy.** Within-user paired AB−TB (`table_8_4c_carb_entry_by_strategy` + `figure_8_4b`): fraction of days with ≥1 carb entry + carb entries/day, Method A (diff = TB−AB) + supportive day-level LMM.
- **Appendix §12.4** (the "both"/messier breakout): `table_12_4a_strategy_cross_tercile` (rank terciles), `table_12_4b_strategy_cross_all5_binary` (all 5 day types), `table_12_4c_strategy_cross_ce0_binary` (CE=0-ref companion), `table_12_4d_strategy_within_user` (within-stratum within-user AB−TB) + figs `12_4a` (tercile) / `12_4b` (all-5) / `12_4c` (CE=0-ref). Per-cohort `run()`/`main()` + `--figures-only`/`--figs` (tags 8_4a/b, 12_4a/b/c).
- **Findings (exploratory):** CE=0/BE≤1 headline — AB ≈ **+8.6 TIR** above TB, interaction **n.s.** (+0.5 [−1.0, 2.1], p=0.51 → gap doesn't differ Low vs High; Method A cross-tab agrees ≈ +8.5 both strata). Part 2 — users log carbs on a **smaller** fraction of TB days than AB days (TB−AB frac −0.12, p≈5e-30, the "surprising direction"); the carb-*rate* metric **diverges in sign** between Method A (−0.62) and the day-level LMM (+0.04) → no directional claim (D5), consistent with the same-day entanglement caveat.
- **Verified:** §8.4 runs end-to-end for adult/pediatric/all (6 tables + 9 figures/cohort); composite gate PASS (equal n_users across a day type's cells); fig 8.4a spot-checked. ⚠️ **same-day entanglement** (strategy is endogenous) + D11 → descriptive only; **inherits §8.3's D12 status** — citable (D12 resolved). Docs: decisions.md D19, architecture.md (§8.4 block + tree), todo.md, report_editor_note.md §8.
- **Follow-up (same day, MJC):** expanded the main **Tables 8.4a (cross-tab) + 8.4b (interaction)** from the headline pair to **all 5 day types** (3 nested NMA + CE>0 + HMA); folded the now-redundant all-5 binary cross-tab (was `table_12_4b`) into Table 8.4a. The main **figure** 8.4a stays the headline CE=0/BE≤1 vs CE>0 (the all-5 figure remains `figure_12_4b`); the Appendix cross-tabs (tercile `12_4a`, CE=0-ref `12_4c`, within-user `12_4d`) stay on the headline pair, matching their figures. All 5 day types' interaction fits converge (incl. the stringent CE=0/BE=0 at n=30: interaction +0.42, p=0.67 n.s.; all 3 NMA arms n.s.). Regenerated all cohorts.

## 2026-06-07 — §8.2/§8.3/§8.4 runnable integration checks implemented

Fleshed out the three stub/missing per-analysis integration runners so every §8 analysis now has an end-to-end check (was: only §8.1). All mirror `run_test_analysis_8_1.py` — a `main(spark)` that ensures statsmodels, builds the synthetic staging pipeline (`run_pipeline.run`, idempotent), loads the analysis module via `run_pipeline.load_analysis_module`, runs it into a **throwaway tempdir** (the real `outputs/analysis_8_X/` are never touched), and asserts the baked-in design. Named `run_test_*` so Databricks runs them as files.

- **`run_test_analysis_8_2.py`** — the `nma_user_known_interaction` 4-cell day_type × delivery_strategy design. (A) per-user recovery on the analysis-ready table: TIR cells 80/70/70/75 on the broadest arm (the TB CE=0 days carry a manual bolus → BE=1, so the clean design lives on CE=0/BE≤∞); (B) `table_8_2a` marginal-cell observed means match an independent re-derivation; (C) `table_8_2b` well-formed + the degenerate-cell guard consistent (converged ⇒ finite interaction, else NaN — not hard-asserted, mirroring §8.1); (D) artifacts.
- **`run_test_analysis_8_3.py`** — the `nma_user_known_low_high_tdd` archetype (30 CE=0/BE=0 days, Low/High TDD). (A) per-user mean-ref Low−High TIR ≈ +15; (B) `table_8_3b` CE=0/BE=0 TIR diff > 0; (C) `table_8_3d` rank-tercile CE=0/BE=0 TIR monotone (Low > High); (D) artifacts (figs 8.3f/8.3g).
- **`run_test_analysis_8_4.py`** — NEW (§8.4 had none). SECONDARY/EXPLORATORY. The synthetic fixture has no user satisfying §8.4's glycemic composite gate (≥30 days × both strategies × both TDD strata in a day type), so the test asserts the **structural + invariant + guard** properties: 8.4a all 5 day types × 2 strategies + the composite same-user-set-gate invariant (equal n_users across a day type's stratum×strategy cells); 8.4b all 5 day types + the thin-cell guard (non-converged ⇒ NaN coefs, n surfaced); 8.4c carb Part-2 (both metrics, ≥1 TB/AB pair — the interaction archetype supplies both strategies); + artifacts.
- **Verified locally** (no Spark, real outputs untouched): built a designed synthetic analysis-ready frame in `/tmp` reproducing the archetypes + a populated §8.4 strategy×stratum user the Databricks fixture lacks, ran §8.2/§8.3/§8.4 into a tempdir, and confirmed every assertion holds — §8.2 interaction LMM converges at −13.29 (≈ design −15), §8.3 Low−High = +15, §8.4 gate kept exactly 2 users in all four CE=0/BE≤1 cells with the interaction converging ≈0 (AB−TB designed equal across strata) and the guard correctly flagging the degenerate arms.
- **Run + PASSED on Databricks (2026-06-07).** All four runners (`run_test_analysis_8_{1,2,3,4}`) execute and pass. Getting green required three fixes: (1) `run_pipeline.pseudonymize_uid` — the runners filtered archetypes by raw `_userId`, but D16 pseudonymizes `_userId` (salted SHA-256) at the analysis-ready export, so the helper reproduces the hash and the runners now match it (a fresh force-rebuild surfaced this — the stale pre-D16 table had hidden it); (2) the §8.1 runner read the renamed `method_a_contrasts.csv` → `table_8_1a_expanded.csv`; (3) `windowed_matched_means` now returns its `{ep}__nma`/`{ep}__cmp` columns on an empty arm so §8.1's windowed HMA builders don't KeyError on the HMA-less fixture (a real robustness fix, hardens the prod pipeline too). Docs: todo.md, architecture.md (testing-harness note).
- **Known coverage gap (follow-up):** the synthetic fixture is too sparse for the LMMs — every cell has <2 distinct users, so all §8.2/§8.3/§8.4 interaction/stratum fits degenerate to `converged=False`; the runners validate design-recovery + structure + guard, not a converged fit. See the HMA-archetype planning prompt below.

## 2026-06-07 — Planning prompt: HMA archetype + LMM-convergence test coverage

Ready-to-use prompt for the next session (mirrors the §8.3×strategy prompt style). Goal: make the integration tests exercise the HMA code paths and a CONVERGED LMM fit, and add direct LMM-helper unit tests.

> **NMA testing — add HMA coverage + make the LMMs actually fit.** Read `testing/integration/build_synthetic_nma_bddp.py` (the archetype builders + the `dob`/`min_days`/`gender` dicts at the top) and `testing/integration/test_build_synthetic_nma_bddp.py` (the per-archetype shape unit tests), plus the four `run_test_analysis_8_*.py`. Problem: the fixture has (a) **no CE≥3/BE≥3 days** → every HMA path (§8.1 column + windowed; §8.2 §12.2 interaction; §8.3/§8.4 HMA arm) degrades to empty, and (b) **≤1 distinct user per designed cell** → every §8.2/§8.3/§8.4 LMM is degenerate (`converged=False`). Task: (1) add an **`nma_user_known_hma`** archetype — a user with **CE≥3 AND BE≥3** days (manual carb+bolus entries), enough days + both delivery strategies (AB days ≥3 autoboluses, TB days 0) so it exercises §8.1's HMA column/windowed contrast and §8.2's §12.2 HMA×strategy interaction; add its `dob`/`min_days`/`gender` dict entries + a shape test in `test_build_synthetic_nma_bddp.py`. (2) Add a **2nd user per design archetype** (`nma_user_known_interaction_2`, `nma_user_known_low_high_tdd_2`, an HMA pair) so each `(day_type × strategy)` / stratum cell clears the **≥2-distinct-user** guard and the LMMs CONVERGE; keep the cell TIRs identical to the originals so the design is unchanged. (3) Extend the runners: §8.2 assert the broadest-arm interaction `converged=True` and the coef ≈ the baked-in design (−15-ish); §8.3 assert `table_8_3c` `lmm_tdd_stratum` `converged=True` + coef ≈ +15; §8.4 assert ≥1 day type's interaction `converged=True` (composite gate keeps the ≥30-day strategy×stratum users). (4) Add **unit tests** in `testing/analysis/test_statistics.py` for `lmm_day_strategy_interaction` / `lmm_tdd_stratum` / `lmm_arm_contrast` — feed a small designed day-level frame (≥2 users, known cell means), assert recovered coef/sign/≈magnitude + the degenerate-cell guard (no Spark). Caveat: keep the existing archetypes' designs intact so §8.1's `nma_user_known_paired_diff` 80/70 + aggregation assertions still hold; rebuild with `run_pipeline.run(spark, force=True)` and re-run all four.

**Part 1 done (2026-06-07) — HMA archetype + HMA-LMM coverage.** Added the `nma_user_known_hma` + `nma_user_known_hma_2` pair to `build_synthetic_nma_bddp` (30 days each: 6 CE=0/BE=0 AB + 24 HMA days at CE≥3/BE≥3, split 12 AB / 12 TB; `make_bolus_events(n_meal=3, n_non_meal=3, n_autobolus=5|0)` ⇒ CE=3 / BE=6 / strategy by autobolus count). Wired DEMOGRAPHICS / ARCHETYPE_DAYS / USER_GENDER (all now 13 entries); added a shape test and fixed the drifted shape file's stale "10 users" count (→ 13) + made its parametrized lists `sorted(nb.ARCHETYPES)` so they self-sync (the other ~9 shape-test failures are pre-existing helper drift — the autobolus subType changed from `automated` to `normal`+HK-flag — left for the prior-scaffold-test cleanup). `run_test_analysis_8_1`/`8_2` now assert the HMA-vs-CE>0 LMM (`table_12_1b`) and the §12.2 HMA×strategy interaction (`table_12_2a`) **converge on TIR**. Verified locally on a /tmp frame (both converge with the pair; `create_table_8_1b` HMA + `a82.run` §12.2). **Part 2 still open:** the MAIN-arm (non-HMA) interaction/stratum LMMs need their own 2nd-user pairs (`*_interaction_2`, `*_low_high_tdd_2`) + the LMM-helper unit tests.

## 2026-06-07 — HMA passed on Databricks; test-suite cleanup; run_all_tests wired

- **HMA build force-rebuilt + re-run on Databricks — green.** §8.1 and §8.2 both pass with the HMA pair; the staging classified the synthetic HMA days correctly (`NMA=2` on the CE≥3/BE≥3 arm) and the §8.1 HMA-vs-CE>0 LMM + §8.2 §12.2 HMA×strategy interaction converge on TIR. **Side effect:** the §8.2 **main** (non-HMA) interaction now converges too — the HMA-TB days are CE>0 days on temp_basal, giving the previously-degenerate `CE>0 × TB` cell its 2nd distinct user. So §8.3's `lmm_tdd_stratum` + §8.4's interaction are the only main-arm LMMs still degenerate (the `*_interaction_2` / `*_low_high_tdd_2` work).
- **Prior-scaffold test cleanup.** Deleted the obsolete `testing/analysis/test_tdd.py` + `test_classification.py` (they `import`ed the deleted `tdd`/`classification` modules — that logic is now Spark staging SQL, covered by the integration runners — and were aborting collection of the whole `testing/analysis/` dir). Fixed the 8 drifted `test_build_synthetic_nma_bddp.py` assertions + 1 in `test_nma_test_helpers.py`: autoboluses are `subType='normal'` + an HK AutomaticallyIssued payload flag (not `subType='automated'`) → count by the flag (`_autoboluses`/`_manual_boluses`); basal rate is in the `rate` column not `normal`; `_to_bddp_row` expands to `NMA_BDDP_COLUMNS`. The non-Spark unit suite is green.
- **`run_all_tests.py` wired** (was a `NotImplementedError` stub) — a two-layer runner: (1) pytest the non-Spark `test_*.py` (so the assertions actually run, unlike a bare `runpy` import); (2) on Databricks, also pytest the `data_staging/` Spark tests + `runpy` the four `run_test_analysis_8_*.py` runners. Local `python testing/run_all_tests.py` runs layer 1 only (layer 2 skipped with a note); non-zero exit on any failure.
- **New test todos recorded:** the TDD **HealthKit-vs-Loop-direct** two-stream + dedup coverage gap (the fixture only emits the HealthKit stream; the Loop-direct `payload.deliveredUnits` fallback, the commanded-vs-delivered subtlety, and the nearest-minute dedup are untested), and the `data_staging/` Spark stubs (rewire vs delete). Docs: todo.md (Tests section), architecture.md (testing-harness note).

## 2026-06-07 — Figure layout (prefix/gap/4×2 merge) + §8.3 gate & age-snapshot confirms

Resolves all four `developer_note.md` §0 asks (the report editor's "before testing" checklist).

- **Figure titles & layout (ask #1).** New `utils.plotting.render_4x2_grid` helper. (a) Removed the baked-in "Figure X.Xy" in-image prefix from every figure across §8.1–§8.4 — the report **caption** is now the single source of the number (fixes the old report Fig 8.2b/8.2c showing in-image "8.2c"/"8.2d"); (b) tightened the suptitle→panel gap; (c) merged the legible line/bar/Δ-histogram dual 2×2 grids — and the 5-arm §8.1 per-user violins (8.1b/12.1b) — into a single 4×2 — `figure_<id>_4x2.png` for 8.1b, 12.1b, 8.1c, 12.1c, 8.2c, 8.3b, 8.3g, 12.3i, 8.4a, 12.4a, 12.4b, 12.4c; the denser violins (8.2a 10-cell, 8.3a/8.3f 6/9-group, 12.3a/c/g) and single-panel figures stay as-is. Dropped the now-dead `fig_id` params + their call-site args. Output-contract change documented in `report_editor_note.md §0` (re-embed filename map) + `developer_note.md §2`; the four `run_test_analysis_8_*` figure-count asserts updated to the merged filenames. Regenerated + visually verified every merged figure legible; a clean full §8.4 run confirms a fresh dir emits only the 4×2 (no stale grids); non-Spark suite green; all edited files compile.
- **§8.3/§12.3 day-count gate (ask #2) — CONFIRMED intended, not a stray filter.** The figure 8.3e/12.3h drop (overall n 877,350 → 580,136, entirely in the CE>0/HMA arm 793,525 → 496,311; CE=0 unchanged at 83,825) is `data_loader.restrict_comparator` — the same CE=0-contributing-cohort restriction used throughout §8.1 (CE>0/HMA flags zeroed for users with no CE=0 day; figure 8.3e then drops those `dotcat==""` rows). Recomputed off the snapshot: with-gate 580,136 (83,825 + 496,311) and without-gate 877,350 (793,525) match the reported numbers **exactly**. Reviewer-Note 4 closes.
- **`nma_user_day_age` parity (ask #3) — confirm script added.** `data_staging/confirm_nma_user_day_age_parity.py` (dual-mode local/Databricks) prints raw + eligible by-`is_pediatric` aggregates. Age is deterministic (`DATEDIFF(local_day, dob)/365.25` as of the measurement day), so a regen matches unless `loop_recommendations` / `bddp_user_dates` / the age cutoffs change. Local reference (eligible): adult 1752 users / 696,563 days (mean 40.2), pediatric 516 / 196,169 (mean 12.4).
- **§8.3 figure-letter order (ask #4) — decided: leave as-is** (letters trace 1:1 to the `figure_8_3X_*` filenames = provenance; renaming would churn the builders, the integration globs, and the report cross-refs for a cosmetic gain).
- **Headline NMA arm → CE=0/BE≤1 (D20, per MJC).** Wherever a single NMA arm is featured — §8.1c (`make_paired_delta_grids`), §12.1c (`make_windowed_delta_grids`), §8.2 (`HEADLINE_CLS`, the 8.2c CE>0-line source) — switched `CLASSIFICATIONS[-1]` (CE=0/BE≤∞) → `CLASSIFICATIONS[1]` (CE=0/BE≤1), matching §8.4's existing `HEADLINE_ARM`. In-image titles now name the arm ("headline NMA arm: CE=0 / BE≤1"). All-arms figures/tables unchanged. ⚠️ D5: BE≤1 is a stringent arm (Method A/B diverge) → the directional claim isn't robust there; framed descriptively + D5 caveat (as §8.4). Recorded in decisions.md D20; editor note in report_editor_note.md §0 point 4. Regenerated + verified (8.1c now n=1158, TIR Δ μ=2.5 vs the old BE≤∞ μ=0.6).

## 2026-06-07 — §8.3 single-CE=0-arm sweep + Tidepool palette (D13) + headline-arm D5 reconciliation

Three coupled passes on the figures plus a methodology reconciliation. See decisions.md **D13 update / D5 update (2026-06-07)**.

- **§8.3 single-CE=0-arm sweep.** Every §8.3 figure that featured ONE CE=0 arm previously used the broadest (BE≤∞); applied the rule "binary violin grids → all 5 day types; tercile grids + histograms + the R-distribution → the explicit CE=0/BE≤1 arm":
  - **8.3a** → all 5 day types (3 nested CE=0 + CE>0 + HMA), two 4×1 grids — new `figure_8_3a_5way_violin`. **12.3a** (median ref) + **12.3c** (rolling ref) reuse it → also all 5 day types.
  - **8.3b** (Low−High Δ histogram), **8.3d** (within-user R-distribution), **8.3f** (rank-tercile violins), **12.3g** (CE=0-ref tercile violins) now feature the explicit **CE=0/BE≤1** arm (labelled "CE=0/BE<=1").
  - All §8.3 **tables** already break out all 3 nested arms — unchanged. **§8-supp** still uses the broadest arm (`NMA_FLAG=in_ce0_be_inf`) but is OUT of the report — flagged, not changed.
  - Code housekeeping: removed dead `strata_inf`; renamed the builder's misleading `strata_inf` param → `ce0_strata`; added `ce0_label`/`ce0_flag` params so only 8.3f/12.3g feature BE≤1 (12.3a/c stay all-5).
- **`DAY_TYPE_COLORS` palette: green ramp → Tidepool-blue ramp (a D13 update, per MJC).** The 3 nested CE=0 arms now read dark Tidepool blue `#1f3a93` (BE=0) → brand blue `#607cff` (the headline BE≤1) → light blue `#aab8ff` (BE≤∞), instead of the old green ramp whose mid step was the TIR/70-180 green (an unneeded in-range cue for day-type figures). **CE>0 grey + HMA bronze unchanged.** Applies to every day-type figure (§8.1b/12.1b violins, §8.2a violins + 8.2c lines, §8.3a/8.3e/8.3g + 12.3a/12.3c/12.3i, §8.4 strategy bars); the range-coloured figures (stacked bars 8.1a/12.1a/8.2d/8.3c; Δ-histograms 8.1c/12.1c/8.3b; the 3-arm strata violins 8.3f/12.3g; 8.3d) keep their endpoint/range colours. Regenerated all three cohorts (adult/pediatric/all).
- **§8.1 per-user violins 8.1b/12.1b merged to a single 4×2** (`figure_<id>_4x2.png`) — extends the earlier 4×2 merge set to **12 figures** (8.1b, 12.1b, 8.1c, 12.1c, 8.2c, 8.3b, 8.3g, 12.3i, 8.4a, 12.4a, 12.4b, 12.4c).
- **D5 reconciliation (supersedes the earlier "stringent → descriptive-only" framing on the headline).** Per the decisions.md D5 update (2026-06-07): on the post-D7-regen snapshot, Method A and Method B **concur in sign on all three NMA arms** for TIR / TAR / mean glucose / CV — these directional contrasts are **method-robust**. So the **CE=0/BE≤1 headline** (D20) TIR/TAR/mean-glucose/CV contrasts are method-robust and citable directionally; the earlier "stringent arm → descriptive only / no robust directional claim / indeterminate" language on the headline is removed. The Method-A-vs-B divergence caveat now attaches to the **below-range / hypoglycemia endpoints only** (time <54, time <70), across all arms. (Unrelated uses of "descriptive" — HMA overlapping-category framing, §8.4 carb-rate same-day entanglement, causal-claim language — are correct and unchanged.)

## 2026-06-07 — Test suite finished: convergence pairs, LMM/helper unit tests, data_staging rewire, TDD coverage, cross-checks

Closed every remaining item in the todo **Tests** section, plus two MJC additions (broader unit layer + a cross-check layer). Local non-Spark suite green; the Spark layer (data_staging tests + the four integration runners) is verified on Databricks via `run_all_tests.main(spark)`.

- **§8.3 / §8.4 main-arm LMM convergence.** Added `nma_user_known_low_high_tdd_2` (identical 30-day Low/High clone → `lmm_tdd_stratum`/Table 8.3c gets ≥2 users/stratum) and a purpose-built `nma_user_known_strategy_stratum` + `_2` pair (32 days = 4 cells {Low,High} TDD × {AB,TB}, day type CE=0/BE≤1 — no prior archetype had both strategies × both strata in one day type). Fixture users 13 → 16 (+ `DEMOGRAPHICS`/`ARCHETYPE_DAYS`/`USER_GENDER` + shape tests + the count). Runner asserts: `run_test_analysis_8_2` check (C) tightened to **hard-assert `converged=True`** + clearly-negative interaction; `8_3` asserts Table 8.3c CE=0/BE=0 `converged=True` + coef ≈ +15; `8_4` asserts the CE=0/BE≤1 interaction `converged=True`. `nma_user_known_interaction_2` was unnecessary (the HMA-TB days already gave §8.2's main interaction its 2nd user). §8.4 convergence verified on a designed `/tmp` analysis-ready frame (interaction converges, composite gate keeps 2 users/cell).
- **Unit layer broadened (no Spark).** `test_statistics.py` gained degenerate-input contract tests (single arm level / single stratum / collapsed cell → the helper raises, which the analysis-layer `converged=False` guard relies on). New `test_data_loader.py` (`restrict_comparator`, `filter_cohort`, `prepare_day_level`, `windowed_matched_means` + empty-arm guard) and `test_strata.py` (`_rank_strata` overall/ce0 × binary/tercile, the D12 `_same_user_set_gate` equal-user invariant, `_ce0_strata`, `table_8_3c_lmm`'s degenerate guard vs the 2-user happy path).
- **`data_staging/` rewired + a classifier test.** Deleted the 4 stubs importing deleted modules. Implemented `test_export_user_day_age` / `_classification` / a focused `_analysis_ready` (Loop-version cohort filter + `_userId` salted-SHA-256 pseudonymization + §7.5 TDD reference/ratio + §7.3 strategy) on the proven `test_export_user_day_bolus_counts` pattern (`setup_test_table` → `run` → `read_test_output` → assert, with `teardown_test_tables` in `finally`). Added **`test_export_user_day_bolus_classification`** (none existed) for the manual/automatic classifier — HK flag, dd fallback, dedup signal aggregation. All module-level-skip when Spark is absent (one clean line).
- **TDD HealthKit-vs-Loop-direct + dedup.** Added `make_loop_direct_basal_row` (+ a non-Spark shape test). `test_export_user_day_tdd` now covers HK basal rate×24, bolus sum incl. autoboluses, Loop-direct-only (`SUM(deliveredUnits)` not `rate×dur`; `basal_source='loop_direct'`), both-streams-prefer-HK, and nearest-minute dedup (one bolus written ×3 → counted once).
- **Cross-check layer (`testing/cross_checks/`, per MJC).** One file per PRIMARY analysis (`test_crosscheck_8_{1,2,3,4}.py`), one test per table: each INDEPENDENTLY recomputes a published table cell from the analysis-ready snapshot with dead-simple, self-contained pandas (no shared helpers, no analysis imports — so a pipeline bug can't be mirrored) and asserts it matches the output CSV. Covers Table 1, 8.1a/8.1b, 8.2a/8.2b, 8.3a/8.3d, 8.4a/8.4b (Method-A means + refit LMMs + the rank/same-user-set-gate cells). All recomputes matched the pipeline (often to the 4th decimal). Supplement §12.x out of scope.
- **`run_all_tests` output (per MJC).** The pytest layer runs verbose + live (`-v --tb=short`) so EVERY test prints `PASSED`/`FAILED`/`SKIPPED` as it runs, with a stack trace on failure. Each integration runner prints one `PASS|FAIL` line — its chatty analysis stdout is captured and dropped on success, and on failure its full Python traceback is printed. A final tally closes the run. (Iterated from MJC feedback: first reduced to pass/fail-only, then restored per-test detail + stack traces.) data_staging tests teardown their tables so none linger.
- **Databricks `/Workspace` `__pycache__` fix.** pytest collection aborted on the cluster because the `/Workspace` FUSE mount can't create `__pycache__` (`OSError: Errno 95`). `run_all_tests` now sets `sys.dont_write_bytecode = True` process-wide and runs pytest with `--assert=plain` + `-p no:cacheprovider`, so neither the import machinery nor the assertion rewriter writes bytecode/cache to the read-only mount.
- **Databricks-run fixes (after the first cluster run).** (1) **Stale fixture:** the runners call `run_pipeline.run(spark)` (idempotent) and were reusing an old analysis-ready table without the new convergence archetypes — `run_all_tests` now force-rebuilds the synthetic fixture once before the integration layer. (2) **Aggregate vs per-user:** the runner convergence asserts were checking the single archetype's baked magnitude (−15 / +15), but the aggregate LMM blends ALL fixture users — relaxed to `converged=True` (§8.2) and `converged=True` + Low>High direction (§8.3); the per-user magnitude is still proven in each runner's check A. (3) **Drifted bolus_counts test:** rewrote `test_export_user_day_bolus_counts` to the current thin-projection signature (it called the old `input_table=` API). (4) **Cross-check gating:** a cross-check SKIPS only when the snapshot (the recompute input, git-ignored) is absent — a bare checkout shouldn't fail; but when the snapshot IS present and the published output table is missing or NaN, the test FAILS (stale/broken pipeline outputs — e.g. not regenerated, or an LMM produced without statsmodels — are worth surfacing, not skipping). `run_all_tests` adds `-rs` so any skip prints its reason. (5) **TDD fixture:** drop all-None columns before `setup_test_table` so Spark can infer the schema.

## Pending / To do (deferred — not yet done)

- **FDA-pipeline `_userId` pseudonymization** — the FDA exports still write raw `_userId`; apply the D16 treatment there too.
- **FDA-pipeline `_userId` pseudonymization** — the FDA exports still write raw `_userId`; apply the D16 treatment there too.

- ~~**⚠️ Understand & fix the §8.3 TDD/tercile results before citing them**~~ — **RESOLVED 2026-06-05** (D12 RESOLUTION): same-user-set-gated two-view rank terciles; high-TDD winsorization evaluated + found immaterial. Rank-tercile outputs citable.

- **Relocate the supplement outputs.** Per MJC: "move the supplement into the analysis folder." The weighting-sensitivity + manual artifacts currently live at `analysis/outputs/analysis_8_1/supplement/`. Clarify the exact intended target (they already sit under `analysis/outputs/…`) before moving; then update `exploratory/lmm_weighting_sensitivity.py` `OUT_DIR` and the path refs in `docs/weighting_sensitivity.md`.
- **Move NMA-day-frequency (§4 secondary objective, bullet 3) out of `analysis_8-1`** into a separate data-overview script. It is a descriptive deliverable in a different plan section than §8.1 (Analysis 1, inferential), so it shouldn't live inside the §8.1 script. Move `create_nma_day_frequency` (and house the other descriptive secondary/overview deliverables — CONSORT flow, PAF distribution) in a dedicated overview module (cf. the prior-scaffold `data_overview.py`); update `run()`, the module docstring, and architecture.md accordingly. Note: Table 8.1c (bullet 4) stays in `analysis_8-1` as it's a labelled §8.1 table.

## Outstanding (to log when answered)

See plan file "Open questions" section.
